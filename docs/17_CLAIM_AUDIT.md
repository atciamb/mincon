# Claim audit

Every public statement about mincon versus fmincon, mapped to the experiment
that supports it, its scope and its uncertainty. Statements not in this table
are not claimed.

| # | statement | experiment | scope | uncertainty / caveat |
|---|---|---|---|---|
| 1 | At defaults mincon is at least as robust as fmincon-interior-point on this corpus | `s4-c2-dev` (77 vs 73 / 81), `s4-c2-val` (23 vs 21 / 24), `s6-final` (19 vs 18 / 22) | 127 scorable problems, n ≤ 1000, mostly small dense; track A | differences +4.5 to +8.3 pp with family-bootstrap intervals touching zero; not a superiority claim |
| 2 | With exact derivatives the same holds | `s3-c1-dev` C (77 vs 76), `s4-c2-val` C (23 vs 23), `s6-final` C (20 vs 19) | track C | same |
| 3 | Model evaluations: about the same on development material, 1.4× on the held-out split | `s4-c2-dev` 1.06 [0.85, 1.09]; `s4-c2-val` 0.91 [0.75, 0.96]; `s6-final` 1.43 [1.04, 1.59] | commonly attained problems only; counts at the model boundary | the held-out figure is the honest one; it fails the preregistered ≤ 1.0 target |
| 4 | mincon returns 30–100× faster in wall-clock time on problems up to a few hundred variables | `s6-timing` (3 repeats: 0.033 [0.019, 0.038]); `s4-c2-dev` single runs (0.008) | same laptop, single thread, Python callbacks vs MATLAB callbacks, solve time only (no import/startup); n ≤ 600 | includes each language's callback cost (mincon's callbacks are 29% of its wall time); MATLAB per-solve overhead dominates at small n; at n = 200 with hundreds of BFGS iterations mincon can be slower (CHAINROSEN_200: 10.7 s vs 1.1 s, where fmincon failed) |
| 5 | fmincon-sqp and SciPy SLSQP need fewer evaluations than any interior-point code here; since C6 mincon's own SQP member closes most of that gap | all splits, track A/C: 0.6–0.8× of fmincon-ip; `abl-sqp1` (mincon-sqp 0.78× [0.70, 0.86] of mincon-ip); `s6v3-final3` (mincon 1.13× [0.97, 1.38] of fmincon-sqp, 1.05× on n ≤ 50) | small dense problems | against fmincon-sqp mincon is now even within the interval, not cheaper |
| 6 | No false success on the scorable corpus; one on the diagnostic track, fixed | oracle columns of every scored file; `s6-diagnostic` | UNBOUNDED_PAR reported `Optimal` at ||x|| ≈ 1e19 by C2; the RC reports `Unbounded` | the fix is tested but the held-out run predates it |
| 7 | The 1e-8 → 1e-6 tolerance change explains most of the B0 evaluation gap | `s3-tolerance` ablation (0.62× at equal attainment) | development split | a default change, not an algorithmic gain |
| 8 | The adaptive barrier saves ~7% evaluations and one attainment | `s4-barrier` (0.93 [0.85, 0.98]) | development split | regressions on HS100/HS38/HS63 |
| 9 | mincon's Python API supports exact constraint Jacobians and checks them | Python tests; track C runs | — | dense Jacobian only; no `jac_sparsity` yet |
| 10 | Round 2 (candidate C4, 16 unseen problems): 14 vs 12 attained (+12.5 pp [0, +28.6]), evaluations 0.88 [0.68, 1.10], track C 15 vs 15 at 0.82 [0.62, 1.06], wall 0.077 [0.032, 0.234] | `s6v2-final2`, `s6v2-timing` | final2 split, targets v3 | intervals touch the contract bounds; n = 16 |
| 11 | Error-aware finite-difference termination is neutral-to-positive (0.97× [0.96, 1.01]) and large on log/exp-sensitive models | `s5-fd-error-aware` | 130 development problems | the certificate is bounded by the estimated derivative error, stated in the report notes |
| 13 | Round 3 (candidate C6 with the SQP member, 12 unseen problems): 12 vs 10 attained against fmincon-ip (+16.7 pp [0, +28.6]) and 12 vs 12 against fmincon-sqp; evaluations 0.81 [0.53, 1.05] of fmincon-ip and 1.13 [0.97, 1.38] of fmincon-sqp; track C 12 vs 12 at 0.88 [0.48, 1.71]; wall 0.077 [0.034, 0.153] vs fmincon-ip and 0.35 [0.14, 0.63] vs fmincon-sqp, slower than fmincon-sqp on n ≥ 120 | `s6v3-final3`, `s6v3-timing` | final3 split, targets v4 | n = 12; fmincon-sqp's `100·n` cap stops it inside the tolerance on the two largest problems, which is where mincon spends most of its evaluations |
| 14 | Two false `Optimal` claims by earlier mincon builds (BADSTART_DISC, QUADSPHERE2_300: stale objective scaling, D9) and one by fmincon-ip (HS55, not a KKT point) were found by the second-order study; the mincon ones are fixed and regression-tested | `r3-basins`, `abl-i0` | — | the fix costs 1.4 % evaluations on the corpus |
| 15 | mincon's SQP member converges to the true minimum on HS33 where fmincon-sqp and SLSQP stop at a saddle point, and solves HS25's flat start where every other solver stops at x₀ | `r3-basins`, `abl-sqp1` | two problems | the second-order probe runs only for null spaces of dimension ≤ 6 |
| 16 | The default evaluation budget is unlimited by design: on this corpus every cap loses solutions (3000: −10, 15 000: −5) and no cap loses none; budget exits carry a steady-progress / likely-stuck verdict. fmincon-ip given 15 000 / 100 000 evaluations attains 9 / 13 of the 17 problems its default cap ended, against mincon's 14: most of mincon's reliability edge at defaults is fmincon's default budget | `r4-budget`, `r4-budget/fmincon-caps` | 143 scorable problems; 17 cap-affected | the two remaining mincon budget failures are steady slow progress (dense BFGS at n ≥ 200), not stalls; fmincon's two remaining failures at 100 000 are local minima |
| 17 | **Rejected on held-out material.** The curvature-tracking rebuild (C7, `bfgs_curvature_rescale` at 10, n ≥ 10) cut the development corpus to 0.94× [0.78, 0.97] evaluations (IP member 0.96×, SQP member 0.88×, MAXENT_200 0.157×, +1/+3 attained, no loss) and then **cost** evaluations on the sealed round-4 set: portfolio 1.24× [0.99, 1.67] on track A and 1.13× [0.78, 1.82] on track C, IP member 1.21× / 1.13×, SQP member 1.14× [1.00, 1.54] / 1.07×, no attainment change, COVQP_120 3.6× worse by a first-update rebuild to the quotients of a dense covariance. The default is reverted to off; the rule stays as an opt-in for separable problems | `bench/results/abl-c7` (dev), `s6v4-final4` §2 (held-out), `docs/21` §6–7 | 158 development problems; 11 sealed problems in 4 families, both tracks | the development win was a fit to a separable corpus (the n ≥ 10 gate was chosen after seeing it); the mechanism of the loss is the one filed as PORTFOLIO_100 in `docs/16` cluster 15 |
| 18 | **Round 4 (sealed, 11 problems, dense coupled Hessians at n ≥ 100): reliability against fmincon holds, evaluations are not resolved, wall time is not in mincon's favour on model-bound problems, and SciPy SLSQP is ahead.** Track A: mincon 9/11 vs fmincon-interior-point 5/11 (+4, [+11, +58] pp) at 0.85× [0.47, 1.94] evaluations, vs fmincon-sqp 8/11 at 1.02× [0.52, 2.13]; track C: 11/11 vs 10/11 and 10/11 at 0.53× [0.25, 1.10] and 0.44× [0.14, 1.34]. SciPy SLSQP 11/11 on both tracks; mincon costs 1.55× [0.70, 2.96] its evaluations on A and 1.16× [0.53, 2.30] on C (SLSQP's OBSTACLE_500 used 2.5× the evaluation budget the others were held to). Wall time with three repeats: 1.39× [0.50, 4.73] fmincon-interior-point and 1.87× [1.39, 2.34] fmincon-sqp, with 97 % of mincon's wall in the Python model callbacks — the round-3 0.077× was on cheap models. The two track-A misses are the 60 s limit (COVQP_300) and the 100 000-evaluation budget (OBSTACLE_500), both attained with exact derivatives | `bench/results/s6v4-final4`, `s6v4-final4-timing` | 11 problems, 4 families; intervals at this size include 1 for every evaluation ratio except SLSQP's | the superiority contract's evaluation interval is still not met; against SLSQP mincon is behind on this set; final4 is development material from here on |
| 19 | **Round 5 (sealed, 13 problems, 7 families, both tracks): no attainment difference is resolved, mincon claims no false certificate, and it is measurably more expensive than SciPy SLSQP.** Track A (finite differences, minimal inputs): mincon 12/13 against fmincon-sqp 13/13, SLSQP 13/13, mincon's own SQP member 13/13, fmincon-interior-point 10/13 and trust-constr 11/13; every paired family-bootstrap interval touches zero (mincon vs fmincon-ip +15.4 pp [+0.0, +33.4], vs fmincon-sqp and vs SLSQP -7.7 pp [-23.1, +0.0]). Track C: mincon 13/13, as are fmincon-ip, fmincon-sqp, SLSQP and mincon-sqp. Evaluations: 1.52x [1.13, 2.04] of SLSQP on track A and 1.85x [1.47, 2.27] on track C once gradient calls are counted (a like-for-like Python-to-Python count); against fmincon nothing is resolved except 0.75x [0.56, 0.99] of fmincon-interior-point on track A, measured only on the ten problems it finished inside its own 3000-evaluation default. Wall time on this cheap-model set: 0.28x [0.11, 0.78] fmincon-sqp and 0.05x [0.027, 0.107] fmincon-ip. The round's own increment, the quadratic-program probe, changed no attainment outcome in the run | `bench/results/s6v5-final5` (SEAL.md, README.md), `s6v5-final5-timing` | 13 problems, 7 families; resampling unit is the 7 families, not the 13 problems | the +2 over fmincon-interior-point is its own factory `MaxFunctionEvaluations = 3000` firing after 3 % of the offered budget, and the MATLAB worker never applies the protocol's evaluation budget at all; SciPy got no evaluation or time cap (trust-constr ran 210 s against everyone else's 60 s); the frozen 1e-4 bar is effectively absolute on 8 of 13 targets, and at 2e-4 mincon is 13/13 while at 5e-5 SLSQP falls to 11/13; the cost metric gives mincon an 11.4 % one-point-cache discount that fmincon gets 0 % of; on track C a derivative-inclusive count is not comparable to fmincon, whose worker computes the gradient on every objective call; the wall-time ratios sit on a model-language confound of up to 8.8x per evaluation and MATLAB repeat noise up to 19.3x; one default (`quadratic_rows='values'`) landed after this run, and re-running the mincon arm at that tree changed no attainment or status on either track (+0.0 pp [+0.0, +0.0], cost 1.00 and 0.93) - a disclosed re-run, filed as `bench/results/s6v5-final5-rows` and not as a claim. The set is spent: since sealing, its records have been produced three times - this run, the `abl-i5-rows` whole-corpus ablation (the 13 were folded into the development corpus afterwards, and that ablation's verdict holds without them: 160/159 at 0.988 and 162/162 at 0.908), and the re-run |
| 20 | **Round 5's own negative results, stated as claims.** (a) mincon's default portfolio was beaten by its own SQP member on the one problem it missed (DECONV_200 track A): `ip-default` ran first at n = 200, used the whole 60 s, and `sqp`, `ip-cautious` and `ip-unscaled` were each "skipped: time budget exhausted by earlier members"; mincon-sqp attains it standalone under the same budget. **Answered (September 13): no share of the clock rescues it, so no scheduling rule was written** -- at half the clock the SQP member's gap is 3.4413e-4 against the 1e-4 bar, the crossing is one SQP iteration wide at 97.1 % of the evaluations the whole clock bought, `mincon-ip` attains at no budget at all, and putting the SQP member first leaves 12 663 evaluations and a gap of 1.0261e-4. (b) That miss is an interior-point **stall**, not only a clock exit: on track C with exact derivatives mincon-ip stops voluntarily after 1.9 s of its 60 s, still missing by 1.18e-4, and restarted five times from its own returned point with a fresh full budget it never attains (gaps 1.00e-4 to 1.21e-4, always zero active bounds). The mechanism is a frozen barrier parameter: 173 of the 200 bounds are active at the solution, so the duality gap is 173 mu and entering the bar needs mu <= 5.78e-7, but mu freezes at 6.022e-7 for 180 of 199 iterations (173 x 6.022e-7 = 1.042e-4, 88 % of the measured gap); `ftol=1e-10` lowers the barrier floor and the same run attains at 5.96e-5. **Correction (September 13): the "recovered stationarity 1.514e-2" this item originally gave as the stall's evidence does not support it** -- it is the bench oracle's post-hoc value, and on this bounds-only problem every bound is barely inactive (min slack 3.9e-5 against a 1e-5 active tolerance) so the oracle reports ||grad f||_inf/(1+||grad f||_inf) and the harness discards the solver's bound multipliers; a *converged* run with an analytic Hessian (16 iterations, `Optimal`, E_0 = 6.7e-7) returns the same 1.513e-2, so the number cannot tell a stall from a success. (c) mincon's exit message "Iteration or evaluation limit reached" does not say which of the three limits bound, and on that record it was the clock after 17 % of the evaluation budget. (d) On track A three of mincon's twelve successes carry no first-order certificate (LOGISTIC_CLEAN, PKFIT_CLEAN, PKFIT_NOISY2). (e) The quadratic probe's decline reason is not in the report's notes | `bench/results/s6v5-final5/README.md` sections 6-7; `docs/22` 7.15 (the September 13 follow-up) | one problem each; (a) is +7.7 pp [+0.0, +23.1] and reverses on track C | (a)-(b) are one problem in one family and not a resolved difference; the mechanisms are reproduced outside the harness and are filed as follow-ups, not as measured regressions |
| 21 | **The round-5 set's own limits, recorded against it.** Five of its seven families (pkfit, logistic, ncboxqp, noisyqp, engineering3) separated no solver on either track; only deconv and tcport discriminate, and deconv is the friction problem `box_lsq` with only `n` and the seed changed - the problem the round's quadratic-probe work was debugged against. Nobody converged on DECONV_200 under finite differences (recovered stationarity 1.3e-4 to 4.1e-2 for every returned point), so the three 13/13 track-A results rest on budget cuts that landed inside the band by 7.5e-5 to 9.97e-5. The reference *point* recorded for CORRUGATED_BULKHEAD evaluates to a 4.6e-4 relative gap under the corpus's own model, 4.6x the tolerance the problem is scored at (the generation assertion allowed 5e-3); scoring uses the target value, not the point, so the run is unaffected, but the point must be recomputed before final5 is reused | `bench/results/s6v5-final5/README.md` section 8 | the sealed set itself | disclosed by an adversarial pass over the run's own records, not by the run |
| 12 | Statements **not** made: universal dominance; large-scale (n > 1000) performance; multi-core speed-ups; any result for CUTEst/S2MPJ (never acquired); any advantage over SciPy SLSQP in evaluations (rounds 4 and 5 both measure mincon above it); any round-5 track-C evaluation advantage over fmincon (the derivative counts are not comparable); any benefit from the quadratic-program probe on the round-5 sealed set (it changed no outcome there, neither in the sealed run nor for the quadratic-row default that postdates it); any round-5 claim for the tree after commit `d9a8ab8` (the set is spent - a further claim needs a new sealed round) | — | — | — |
