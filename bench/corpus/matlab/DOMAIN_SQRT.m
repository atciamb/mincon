function p = DOMAIN_SQRT()
% DOMAIN_SQRT: generated by bench/corpus/generate.py from the SymPy definition. Do not edit.
% source: closed form, bench/corpus/adversarial.py
p.name = 'DOMAIN_SQRT'; p.family = 'adversarial'; p.n = 2; p.m = 0;
p.x0 = [4.0; 0.0];
p.xl = [0.25; -Inf]; p.xu = [Inf; Inf];
p.cl = zeros(0, 1); p.cu = zeros(0, 1);
p.ref_f = 0.5;
p.checksum = '8b4463eb61e65d2f';
p.f = @DOMAIN_SQRT_f; p.grad = @DOMAIN_SQRT_g; p.c = @DOMAIN_SQRT_c; p.jac = @DOMAIN_SQRT_J;
end

function f = DOMAIN_SQRT_f(x)
f = sqrt(x(1)) + (x(2) - 1).^2;
end

function g = DOMAIN_SQRT_g(x)
g = zeros(2, 1);
g(1) = 1./(2*sqrt(x(1)));
g(2) = 2*x(2) - 2;
end

function c = DOMAIN_SQRT_c(x)
c = zeros(0, 1);
end

function J = DOMAIN_SQRT_J(x)
J = zeros(0, 2);
end
